# چک‌لیست نسخه بعدی سیاهه

## هدف این سند
این سند برای هم‌راستا کردن نسخه بعدی با هویت واقعی محصول است:
سیاهه یک «واقعه‌نگار/منشی فضایی» است که تجربه استفاده‌اش باید برای کاربر آشنا و شبیه پیام‌رسان باشد، اما مدل دامنه و معماری آن باید متناسب با ثبت تاریخچه کار، رخداد و تجربه طراحی شود.

## خروجی مورد انتظار نسخه بعدی
- کاربر حس کند داخل یک پیام‌رسان آشنا کار می‌کند.
- در عین حال مفاهیم دامنه برای تیم فنی شفاف، درست و بدون ابهام باشند.
- شماره تلفن کاربر برای دیگران آشکار نشود.
- کاربر بتواند پروفایل واقعی داشته باشد: نام نمایشی، نام کاربری، آواتار.
- هر ورودی بتواند چند فایل یا چند تصویر یا ویدیو داشته باشد.
- ساختار کد بک‌اند و اندروید برای رشد نسخه‌های بعدی قابل اتکا باشد.

## 1. تصمیم‌های پایه محصول و واژگان
- [x] واژگان محصول نهایی مشخص شود: آیا در UI از «فضا / پیام / چت» استفاده می‌کنیم یا از «فضا / ثبت / تاریخچه».
- [x] واژگان دامنه داخلی مشخص شود: آیا مدل‌های فنی باید `Message` بمانند یا به `Entry` / `Record` / `LogEntry` تغییر کنند.
- [x] تصمیم بگیریم آیا «شبیه پیام‌رسان بودن» فقط در UI است یا در API و مدل هم باید منعکس شود.
- [x] یک glossary کوتاه ثبت شود تا تیم محصول، بک‌اند و اندروید از یک زبان مشترک استفاده کنند.
- [x] مشخص شود «فضا شخصی» همچنان feature اصلی است یا فقط یک onboarding convenience.

## 2. بازطراحی مدل دامنه
- [x] مدل `User` از وابستگی مفهومی به شماره تلفن جدا شود.
- [x] برای کاربر این فیلدها اضافه یا نهایی شود: `display_name`, `username`, `avatar`, `bio` یا `about`.
- [x] تصمیم بگیریم `phone` فقط credential ورود باشد یا بخشی از پروفایل.
- [x] حریم خصوصی کاربر در مدل و serializer enforce شود تا شماره تلفن هرگز برای سایر اعضا برنگردد.
- [x] مدل فعلی `Message` از نظر نام بررسی شود:
  - اگر قصد داریم محصول در ذهن تیم «واقعه‌نگار» بماند، نامی مثل `Entry` یا `LogEntry` امن‌تر است.
  - اگر قصد داریم تجربه و توسعه کوتاه‌مدت شدیداً شبیه messenger بماند، می‌توان موقتاً `Message` را نگه داشت ولی باید در اسناد دامنه روشن شود.
- [x] اگر rename انجام می‌شود، migration و refactor کامل برای backend, API, Android و docs برنامه‌ریزی شود.
- [x] مدل attachment از خود message/entry جدا شود.
- [x] برای attachmentها یک مدل مستقل تعریف شود، مثل `EntryAttachment` یا `MessageAttachment`.
- [x] قابلیت چند attachment برای هر entry/message اضافه شود.
- [x] نوع فایل‌ها پشتیبانی شود: image, video, audio, document, generic file.
- [x] metadata فایل‌ها ذخیره شود: `mime_type`, `size`, `width`, `height`, `duration`, `original_name`.
- [x] ترتیب attachmentها در یک entry حفظ شود.
- [x] برای آینده، امکان reply/quote/forward یا pin روی entryها فقط به‌صورت تصمیم محصولی ثبت شود، نه لزوماً پیاده‌سازی فوری.

## 3. معماری بک‌اند
- [x] بازبینی شود که `apps.accounts` بیش از حد شلوغ شده و دامنه‌های نامرتبط را در خود جمع کرده است.
- [x] ماژول‌های بک‌اند بر اساس دامنه تفکیک شوند. یک گزینه محتمل:
  - `apps.identity`: کاربر، پروفایل، احراز هویت، session/login metadata
  - `apps.spaces` یا `apps.spaces`: فضا/فضا و عضویت
  - `apps.timeline` یا `apps.entries`: entry/message و attachment
  - `apps.common`: فقط shared utilities
- [x] منطق OTP و token از منطق فضا و پیام جدا شود.
- [ ] serializerها به تفکیک دامنه و use case مرتب شوند، نه یک فایل واحد برای همه‌چیز.
- [x] permission logic از viewها بیرون کشیده شود و به policy/service/permission classهای روشن منتقل شود.
- [x] منطق دسترسی `owner/admin/view_all/member` در یک لایه واحد متمرکز شود تا در چند endpoint تکرار نشود.
- [x] ruleهای visibility با صراحت بازتعریف شوند:
  - `member` دقیقاً چه چیزی می‌بیند؟
  - `view_all` دقیقاً چه تفاوتی دارد؟
  - عضو غیرفعال چه چیزهایی را همچنان می‌بیند؟
- [x] API کاربر برای جستجوی اعضا با `username` یا شناسه امن طراحی شود تا افزودن عضو وابسته به `user_id` خام نباشد.
- [x] endpointهای media upload و attachment management طراحی شوند.
- [x] برای uploadهای بزرگ، استراتژی مشخص شود:
  - upload مستقیم به backend
  - یا presigned upload به object storage
- [x] pagination برای لیست پیام‌ها/entryها اضافه شود.
- [x] ترتیب و بارگذاری تاریخچه برای chat-like UX بهینه شود.
- [x] جستجو و فیلتر با مدل جدید attachments و sender profile سازگار شود.
- [x] responseهای کاربر و member فقط داده‌های public-safe را برگردانند.
- [x] schema و docs API با مسیرهای واقعی sync شوند.

## 4. معماری اندروید
- [ ] package structure فعلی بازبینی شود تا domain-drivenتر شود و فقط API-driven نباشد.
- [ ] مدل‌های data layer از مدل‌های domain و UI جدا شوند.
- [x] اگر rename دامنه انجام می‌شود، در اندروید هم `SpaceMessage` و مشابه آن به مدل‌های دقیق‌تر تبدیل شوند.
- [x] جریان‌های auth/profile/space/entry/attachment به repositoryها و use caseهای روشن‌تر تقسیم شوند.
- [x] صفحه چت از نظر UI همچنان chat-like بماند، اما semantics آن با «ثبت واقعه» هماهنگ شود.
- [x] UI اعضا از اتکا به `phone` خارج شود و `display_name`, `username`, `avatar` نمایش دهد.
- [x] در فضاهای بیشتر از یک نفر، کنار هر entry آواتار و نام نمایشی فرستنده نشان داده شود.
- [x] در فضا شخصی، UI بتواند compactتر باشد و avatar فرستنده را مخفی یا ساده‌تر کند.
- [x] composer پیام بازطراحی شود تا افزودن چند تصویر/چند فایل/ویدیو را پشتیبانی کند.
- [x] وضعیت upload برای هر attachment به کاربر نشان داده شود.
- [x] preview برای تصویر و فایل قبل از ارسال اضافه شود.
- [x] نمایش attachmentها در timeline/chat برای انواع مختلف فایل طراحی شود.
- [x] امکان مشاهده تمام‌صفحه تصویر و باز کردن فایل‌ها از داخل entry فراهم شود.
- [x] فیلترها و جستجو user-friendly شوند؛ گرفتن `senderId` خام از کاربر تجربه خوبی نیست.
- [x] اطلاعات هویتی کاربر در اپ به‌روزرسانی و cache شود: display name, username, avatar.
- [x] onboarding بعد از OTP تکمیل شود تا کاربر اگر پروفایل ناقص داشت، نام و نام کاربری خود را تنظیم کند.

## 5. پروفایل و حریم خصوصی
- [x] کاربر پس از اولین ورود، پروفایل اولیه خود را تکمیل کند.
- [x] `username` یکتا باشد و برای share/add/search استفاده شود.
- [x] `display_name` قابل تغییر باشد و در UI اصلی نمایش داده شود.
- [x] `avatar` قابل آپلود و ویرایش باشد.
- [x] شماره تلفن فقط برای login و recovery استفاده شود و در API عمومی برنگردد.
- [x] در serializerهای فعلی، تمام جاهایی که `phone` لو می‌رود شناسایی و حذف/محدود شوند.
- [x] policy مشخص شود که چه کسی چه فیلدهای کاربر را می‌بیند.
- [x] اگر دفترچه مخاطب یا invite later مدنظر است، از ابتدا privacy-safe طراحی شود.

## 6. تجربه کاربری نسخه بعدی
- [x] تعریف روشن از «حس پیام‌رسان» نوشته شود تا سلیقه‌ای اجرا نشود.
- [x] موارد پایه messenger-like در نسخه بعدی بررسی شوند:
  - لیست گفتگو/فضا با preview آخرین entry
  - timestamp آشنا
  - avatarها
  - bubble یا cardهایی که حس chat بدهند
  - composer آشنا با دکمه attachment
- [x] لحن UI روشن شود: آیا کاربر «پیام می‌فرستد» یا «رخداد ثبت می‌کند».
- [x] در صورت نیاز، متن‌های UI hybrid شوند:
  - ظاهر آشنا مثل پیام‌رسان
  - مفهوم داخلی: ثبت وقایع و تاریخچه
- [x] صفحه فضاها و صفحه چت از نظر hierarchy بصری بازبینی شوند.
- [x] entryهای دارای چند media/file طراحی اختصاصی بگیرند، نه فقط text + image.
- [x] empty stateها، loading stateها و error stateها برای تجربه حرفه‌ای‌تر بازبینی شوند.
- [x] تاریخ‌ها و زمان‌ها برای فارسی‌زبان‌ها به شکل طبیعی‌تر نمایش داده شوند.
- [x] آیکن اپ اندروید با هویت «واقعه‌نگار / منشی» هم‌راستا شود و از حس generic اندروید خارج شود.

## 7. API و قرارداد داده
- [x] endpointهای `me/profile` برای خواندن و ویرایش پروفایل کاربر تعریف یا بازطراحی شوند.
- [x] API عضویت از وابستگی به `user_id` خام خارج شود و راه جستجو/افزودن با `username` یا invite فراهم شود.
- [x] response مربوط به members شامل avatar, display_name, username باشد.
- [x] response مربوط به message/entry شامل sender public profile باشد.
- [x] API ارسال entry از حالت `text + single image` به `text + multiple attachments` تغییر کند.
- [x] استراتژی backward compatibility مشخص شود:
  - آیا API v2 می‌دهیم؟
  - یا API فعلی را می‌شکنیم؟
- [x] قراردادهای upload/resume/failure برای attachmentها تعریف شود.
- [x] محدودیت‌های اندازه و نوع فایل شفاف و قابل تنظیم باشند، نه hard-coded و مبهم.

## 8. دیتا، migration و سازگاری
- [ ] برای rename مدل‌ها و جابه‌جایی appها برنامه migration واقعی نوشته شود.
- [x] تصمیم بگیریم آیا migration reset مجاز است یا باید روی دیتای موجود migrate کنیم.
- [x] اگر شماره تلفن از public profile حذف می‌شود، داده‌های موجود برای پروفایل عمومی backfill شوند.
- [x] اگر `username` اجباری می‌شود، استراتژی تولید اولیه آن مشخص شود.
- [x] اگر attachment model اضافه می‌شود، فیلد `image` فعلی `Message` به attachment migrate شود.
- [x] داده‌های نمایشی قدیمی اندروید با مدل جدید سازگار شوند یا invalidate شوند.

## 9. تست و تضمین کیفیت
- [x] تست permissionها برای همه نقش‌ها نوشته شود.
- [x] تست privacy نوشته شود تا phone در responseهای عمومی نشت نکند.
- [x] تست create/list/filter برای entryها با visibilityهای مختلف نوشته شود.
- [x] تست upload چند attachment و retrieval آن‌ها نوشته شود.
- [x] تست onboarding پروفایل کاربر نوشته شود.
- [x] تست regression برای OTP/login/token refresh حفظ شود.
- [x] سناریوی دستی end-to-end تعریف شود:
  - ثبت‌نام
  - تکمیل پروفایل
  - ساخت فضا
  - افزودن عضو
  - ثبت چند entry
  - ارسال چند فایل
  - مشاهده تاریخچه از نقش‌های مختلف

## 10. ترتیب پیشنهادی اجرا
- [ ] فاز 1: تثبیت واژگان، مدل دامنه، privacy rules, API contract
- [ ] فاز 2: refactor بک‌اند و migrationهای اصلی
- [ ] فاز 3: پروفایل کاربر و membership search/invite
- [ ] فاز 4: attachment model و upload flow
- [ ] فاز 5: بازطراحی اندروید برای profile/avatar/chat-like timeline
- [ ] فاز 6: polish تجربه کاربری، تست یکپارچه، آماده‌سازی production

## 11. مواردی که همین حالا از کد فعلی نیاز به اصلاح دارند
- [x] `phone` در `UserSerializer`, `SpaceMembershipSerializer`, `MessageSerializer` به‌عنوان داده عمومی استفاده شده و با هدف محرمانگی ناسازگار است.
- [x] `apps.accounts` هم احراز هویت را در خود دارد هم فضا، عضو و پیام را؛ این برای رشد بعدی مناسب نیست.
- [x] مدل `Message` فقط `text + image` دارد و برای چند فایل/چند تصویر/ویدیو کافی نیست.
- [x] UI اندروید در چت و اعضا هنوز `phone` را به‌عنوان هویت اصلی نمایش می‌دهد.
- [x] افزودن عضو با `user_id` خام تجربه محصولی مناسبی ندارد.
- [x] فیلتر فرستنده در UI بر پایه `senderId` خام است و برای کاربر نهایی مناسب نیست.
- [x] نام‌گذاری فعلی هنوز محصول را به سمت «messenger ساده» هل می‌دهد و «واقعه‌نگار تیمی» را کامل منعکس نمی‌کند.

## 12. تصمیم‌های ثبت‌شده از پاسخ‌های اخیر
- [x] در معماری داخلی، شباهت صرف به پیام‌رسان نباید باعث ابهام دامنه شود؛ بنابراین نام‌گذاری باید بر اساس معماری درست انتخاب شود، نه صرفاً آشنایی ظاهری.
- [x] در UI باید از واژه‌ای استفاده شود که به «لاگ / ثبت / واقعه» نزدیک باشد و ذهن کاربر را از مفهوم «پیام شخصی» دور کند.
- [x] `username` باید برای هر کاربر وجود داشته باشد؛ در شروع می‌تواند به‌صورت خودکار و رندوم تولید شود و بعداً قابل تغییر باشد.
- [x] مدیر باید بتواند کاربران را با `username` اضافه کند.
- [x] در نسخه‌های بعدی، کشف کاربر از روی مخاطبان گوشی هم به‌عنوان قابلیت هم‌راستا با پیام‌رسان‌ها در نظر گرفته شود.
- [x] برای نسخه بعدی، افزودن عضو با `username` و پیدا کردن از روی لیست مخاطبین بر لینک دعوت اولویت دارد.
- [x] قابلیت attachment باید از نظر تجربه به سطحی نزدیک به پیام‌رسان‌هایی مثل تلگرام برسد: چند تصویر، چند فایل، ویدیو، و فایل generic در یک entry.
- [x] migration می‌تواند تهاجمی و refactor-friendly باشد؛ دیتای فعلی مانع تغییر ساختار نیست.
- [x] فضا شخصی باید در نمایش ساده‌تر و قابل تشخیص باشد.
- [x] فضا شخصی باید بعداً قابل تبدیل به فضا چندنفره باشد.
- [x] تبدیل فضا شخصی به فضا چندنفره باید irreversible باشد و قبل از انجام، اخطار واضح به کاربر نمایش داده شود.

## 13. تصمیم‌های نهایی تکمیلی
- [x] واژه نهایی UI برای `entry` برابر «ثبت» است.
- [x] در نسخه بعدی، کشف مخاطبین برای یافتن کاربران عضو اپ اجرا می‌شود و invite برای افراد غیرعضو به نسخه بعدی‌تر منتقل می‌شود.
- [x] public کردن فضا شخصی فقط با action صریح «تبدیل به فضا» انجام می‌شود و صرف افزودن عضو کافی نیست.
