## سیاهه: وضعیت فعلی پروژه

### خلاصه
سیاهه یک اپ ثبت وقایع و تجربه‌هاست با ساختار فضایی (Space/Space). تجربه ثبت شبیه چت است و هر پیام شامل متن و نهایتا یک تصویر است. نسخه فعلی روی حداقل‌ها تمرکز دارد: ثبت پیام، مشاهده تاریخچه، جستجو و فیلتر.

### دامنه نسخه فعلی
- ورود با شماره تلفن و OTP
- فضاها: ساخت، لیست، پین/هایـد
- پیام‌ها: ثبت متن، لیست، جستجو و فیلتر
- اعضا: افزودن/مدیریت نقش

### مفاهیم کلیدی
- **Space**: فضای ثبت مشترک یا شخصی
- **Entry/Message**: هر پیام متن + تصویر اختیاری
- **Role**‌ها در فضا: `owner`, `admin`, `view_all`, `member`
- دسترسی‌ها:
  - `view_all`: مشاهده همه پیام‌ها
  - `member`: مشاهده پیام‌های خودش
  - حذف عضو یعنی غیر‌فعال شدن، تاریخچه قابل مشاهده می‌ماند
  - owner امکان حذف ندارد، فقط hide/پین قابل تغییر است

### تکنولوژی
- Backend: Django + DRF
- Android: Kotlin + Jetpack Compose + Hilt + Retrofit
- Storage: Arvan S3 (اختیاری با `USE_S3`)
- Async: Celery + Redis

### ساختار بک‌اند
- اپ‌ها: `apps.accounts`, `apps.common`
- مدل‌های اصلی: `User`, `Space`, `SpaceMembership`, `Message`, `UserClientLogin`
- مهاجرت‌ها ریست شده‌اند و `0001_initial.py` جدید ساخته شده است

### API مهم
- `POST /auth/otp/` و `POST /auth/verify/`
- فضاها: `GET/POST /spaces/`, `GET /spaces/{id}/`
- پیام‌ها: `GET/POST /spaces/{id}/messages/`
- اعضا: `GET/POST /spaces/{id}/members/`, `PATCH/DELETE /spaces/{id}/members/{user_id}/`
- ترجیحات فضا: `PATCH /spaces/{id}/preferences/`

### نکات اجرایی
- در محیط تولید `DEBUG` باید خاموش باشد
- برای اجرا نیاز به Postgres و Redis است
- اندروید روی Compose با تم اختصاصی سیاهه پیاده شده است

### محدودیت‌های فعلی
- تحلیل هوشمند و خروجی کامل تاریخچه فعلا پیاده نشده
- پیام‌ها فقط متن و تصویر (بدون فایل‌های دیگر)
- تجربه ثبت فقط شبیه چت است (بدون فرم ساختاریافته)
